# APRIS Web Chat — Python Dependencies
# Jalankan lokal: pip install -r requirements.txt

# Google Gemini AI
google-genai>=0.3.0

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0

# Production WSGI server (Railway)
gunicorn>=22.0.0

# Environment & timezone
python-dotenv>=1.0.0
tzdata>=2024.1

# RAG & Semantic Memory (ChromaDB)
chromadb>=0.5.0

# PDF Parsing & OCR
PyPDF2>=3.0.0          # ekstrak teks dari PDF digital
pymupdf>=1.24.0        # render PDF scan ke gambar untuk OCR via Gemini Vision

# Web Scraping
beautifulsoup4>=4.12.0
requests>=2.31.0

# Scheduler (Reminders, Daily Brief, Auto-Ingest, Proactive Agent)
apscheduler>=3.10.0
sqlalchemy>=2.0.0

# Google API (Calendar, Gmail, Drive, Tasks, Contacts/People)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0

# Voice: STT (Whisper) + TTS (gTTS) — opsional, install manual jika diperlukan
# openai-whisper>=20231117
# gTTS>=2.5.0

# Notion Integration
notion-client>=2.2.0

# WhatsApp Integration (Twilio)
twilio>=9.0.0
