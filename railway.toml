[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn chat_server:app --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 8 --timeout 120"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
