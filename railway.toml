[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn rag_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
